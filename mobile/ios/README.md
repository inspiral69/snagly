# Open Snagly in Xcode

**Always open:** `Snagly.xcworkspace`  
(or double‑click `Open Snagly in Xcode.command`)

**Do not open:** `Snagly.xcodeproj` — that file is required behind the scenes, but opening it alone causes build errors.
