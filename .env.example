# Local / docs only — never commit real secrets.
# Store production secrets on the Cloudflare Worker:
#   cd workers/snagly-api
#   npx wrangler secret put EBAY_CLIENT_ID
#   npx wrangler secret put EBAY_CLIENT_SECRET

EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
EBAY_ENV=production
